#!/usr/bin/make -f

export CGO_ENABLED=1
export GOCACHE=$(PWD)/../cache

ifndef VERSION
	VERSION = $(shell cat debian/control | grep Standards-Version | cut -d' ' -f2)
endif

ARCH = $(shell uname -m)

%:
	dh $@

build:
	mv kubescape/$(ARCH)/kubescape kubescape
